Gem::Specification.new do |s|
  s.name    = "rails_settings"
  s.version = "0.0.0"
  s.authors = ["Georg Ledermann"]
  s.summary = "Settings is a plugin that makes managing a table of key/value pairs easy."
  s.files   = ["lib/settings.rb"]
  s.add_runtime_dependency "rails", "~> 3"
end
